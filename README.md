# ExploreWithMe: Микросервисная платформа мероприятий

Микросервисное приложение для организации мероприятий с возможностью поиска событий, участия в них и комментирования.

## 🏗 Архитектура проекта

Проект построен по микросервисной архитектуре с разделением на бизнес-сервисы и инфраструктурные компоненты.

### Бизнес-сервисы (Core)

1. **User Service** — управление пользователями
2. **Event Service** — управление событиями, категориями и подборками событий
3. **Request Service** — обработка заявок на участие в событиях
4. **Comment Service** — система комментариев к событиям

### Инфраструктурные сервисы (Infra)

1. **Discovery Server (Eureka)** — сервис регистрации и обнаружения микросервисов
2. **Config Server** — централизованное управление конфигурациями всех сервисов
3. **Gateway Server** — единая точка входа для всех клиентских запросов

### Сервис статистики (Stats)

1. **Stats Server** — сбор статистики просмотров событий

## 🔗 Взаимодействие сервисов

**Поток запроса:**
1. **Клиент** → Gateway Server (порт 8080)
2. **Gateway** → Соответствующий микросервис (через Eureka)
3. **Микросервис** → Config Server (загрузка конфигураций)

**Инфраструктурные компоненты:**
- **Eureka Server** (порт 8761) — регистрация и обнаружение сервисов
- **Config Server** — централизованное хранение конфигураций
- **Gateway Server** (порт 8080) — единая точка входа


### Межсервисная коммуникация:
- **Feign Clients** — для синхронных HTTP вызовов между сервисами
- **Resilience4j** — аннотации @CircuitBreaker и @Retry для повышения отказоустойчивости

## ⚙️ Конфигурации

Все конфигурации сервисов хранятся в **Config Server** в папке `infra/config-server/src/main/resources/config/`:

- `user-service/` — настройки User Service
- `event-service/` — настройки Event Service
- `request-service/` — настройки Request Service
- `comment-service/` — настройки Comment Service
- `stats-server/` — настройки Stats Server
- `gateway-server/` — настройки Gateway Server

## 🔒 Внутренний API

Для взаимодействия между сервисами используется внутреннее API через Feign-клиенты:

### User Service
- `GET /internal/users/{userId}` — получение информации о пользователе
- `GET /internal/users/exists/{userId}` — проверка существования пользователя

### Event Service
- `GET /internal/events/{eventId}` — получение события
- `GET /internal/events/exists/{eventId}` — проверка существования события

### Request Service
- `GET /internal/requests/event/{eventId}/count` — количество подтвержденных заявок на событие

### Comment Service
- `GET /internal/comments/count/{eventId}` — количество опубликованных комментариев к событию

## 📚 Внешний API

Спецификация внешнего API доступна по ссылке:  
https://raw.githubusercontent.com/yandex-praktikum/java-explore-with-me/main/ewm-main-service-spec.json

## 🛠 Технологии

- **Java 21** + **Spring Boot 3**
- **Spring Cloud** — Gateway, Config Server, Eureka, OpenFeign
- **PostgreSQL** — отдельная база данных для каждого сервиса
- **Docker Compose** — контейнеризация баз данных

Все сервисы автоматически регистрируются в Eureka и загружают конфигурации из Config Server.